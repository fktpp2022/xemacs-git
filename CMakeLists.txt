cmake_minimum_required(VERSION 3.16)

project(XEmacs
  VERSION 21.5.36
  DESCRIPTION "XEmacs - The next generation Emacs editor"
  LANGUAGES C
)

set(CMAKE_C_STANDARD 99)
set(CMAKE_C_STANDARD_REQUIRED ON)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

list(APPEND CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/cmake/Modules")

include(CTest)
include(CheckIncludeFile)
include(CheckFunctionExists)
include(CheckSymbolExists)
include(CheckTypeSize)
include(CheckCSourceCompiles)
include(CheckCSourceRuns)
include(CMakePackageConfigHelpers)

include("${CMAKE_SOURCE_DIR}/cmake/XEmacsOptions.cmake")
include("${CMAKE_SOURCE_DIR}/cmake/XEmacsOS.cmake")
# Version info - must be set before XEmacsConfigure
set(XEMACS 1)
set(EMACS_MAJOR_VERSION 21)
set(EMACS_MINOR_VERSION 5)
set(EMACS_BETA_VERSION 36)
set(XEMACS_CODENAME "leeks")
set(XEMACS_RELEASE_DATE "2025-06-14")

if(EMACS_IS_BETA)
  set(EMACS_VERSION "${EMACS_MAJOR_VERSION}.${EMACS_MINOR_VERSION}-b${EMACS_BETA_VERSION}")
else()
  set(EMACS_VERSION "${EMACS_MAJOR_VERSION}.${EMACS_MINOR_VERSION}.${EMACS_BETA_VERSION}")
endif()

set(PROGNAME "xemacs")
set(EMACS_PROGNAME "${PROGNAME}")
set(SHEBANG_PROGNAME "xemacs-script")
set(EMACS_DUMP_FILE_NAME "xemacs.dmp")

# Must compute lowercase BEFORE using it
string(TOLOWER "${CMAKE_SYSTEM_NAME}" CMAKE_SYSTEM_NAME_LOWER)
set(EMACS_CONFIGURATION "${CMAKE_SYSTEM_PROCESSOR}-unknown-${CMAKE_SYSTEM_NAME_LOWER}")

set(EMACS_CONFIG_OPTIONS " '--with-menubars=lucid' '--with-scrollbars=lucid' '--with-widgets=lucid' '--with-mule'")
set(STACK_TRACE_EYE_CATCHER "xemacs_${EMACS_MAJOR_VERSION}_${EMACS_MINOR_VERSION}_b${EMACS_BETA_VERSION}_${CMAKE_SYSTEM_PROCESSOR}_unknown_${CMAKE_SYSTEM_NAME_LOWER}")
set(TYPEOF "__typeof__")
set(NEED_TO_HANDLE_21_4_CODE 1)
set(PREFIX_USER_DEFINED 1)
set(HAVE_UNIX_PROCESSES 1)
set(INHIBIT_SITE_LISP 1)

include("${CMAKE_SOURCE_DIR}/cmake/XEmacsConfigure.cmake")

set(inststaticdir "${PROGNAME}")
set(instvardir "${PROGNAME}-${EMACS_VERSION}")

if(NOT DEFINED CMAKE_INSTALL_PREFIX)
  set(CMAKE_INSTALL_PREFIX "/usr/local")
endif()

set(XEMACS_PREFIX "${CMAKE_INSTALL_PREFIX}")
set(XEMACS_EXEC_PREFIX "${CMAKE_INSTALL_PREFIX}")
set(XEMACS_BINDIR "${CMAKE_INSTALL_PREFIX}/bin")
set(XEMACS_DATAROOTDIR "${CMAKE_INSTALL_PREFIX}/share")
set(XEMACS_DATADIR "${XEMACS_DATAROOTDIR}")
set(XEMACS_STATEDIR "${CMAKE_INSTALL_PREFIX}/lib")
set(XEMACS_LIBDIR "${CMAKE_INSTALL_PREFIX}/lib")
set(XEMACS_MANDIR "${XEMACS_DATAROOTDIR}/man")
set(XEMACS_INFODIR "${XEMACS_DATADIR}/${instvardir}/info")

set(XEMACS_LISPDIR "${XEMACS_DATADIR}/${instvardir}/lisp")
set(XEMACS_SITELISPDIR "${XEMACS_DATADIR}/${inststaticdir}/site-lisp")
set(XEMACS_ETCDIR "${XEMACS_DATADIR}/${instvardir}/etc")
set(XEMACS_ARCHLIBDIR "${XEMACS_LIBDIR}/${instvardir}/${EMACS_CONFIGURATION}")
set(XEMACS_DOCDIR "${XEMACS_ARCHLIBDIR}")
set(XEMACS_MODULEDIR "${XEMACS_ARCHLIBDIR}/modules")
set(XEMACS_SITEMODULEDIR "${XEMACS_LIBDIR}/${inststaticdir}/site-modules")

set(XEMACS_LATE_PACKAGES "${XEMACS_DATADIR}/${PROGNAME}")

message(STATUS "XEmacs ${EMACS_VERSION} \"${XEMACS_CODENAME}\"")
message(STATUS "Installation prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Configuration: ${EMACS_CONFIGURATION}")
message(STATUS "Build type: ${CMAKE_BUILD_TYPE}")

configure_file(
  "${CMAKE_SOURCE_DIR}/src/config.h.in.cmake"
  "${CMAKE_BINARY_DIR}/src/config.h"
  @ONLY
)

configure_file(
  "${CMAKE_SOURCE_DIR}/src/paths.h.in.cmake"
  "${CMAKE_BINARY_DIR}/src/paths.h"
  @ONLY
)

configure_file(
  "${CMAKE_SOURCE_DIR}/lwlib/config.h.in.cmake"
  "${CMAKE_BINARY_DIR}/lwlib/config.h"
  @ONLY
)

file(MAKE_DIRECTORY "${CMAKE_BINARY_DIR}/src")

set(EMACS_AD_H "${CMAKE_BINARY_DIR}/src/Emacs.ad.h")
set(EMACS_AD_SOURCE "${CMAKE_SOURCE_DIR}/etc/Emacs.ad")
set(AD2C_SCRIPT "${CMAKE_SOURCE_DIR}/lib-src/ad2c")

set(GEN_EMACS_AD_SCRIPT "${CMAKE_BINARY_DIR}/gen_emacs_ad.sh")
file(WRITE "${GEN_EMACS_AD_SCRIPT}"
"#!/bin/sh
echo '/*\tDo not edit this file!'
echo '  \tAutomatically generated from ${EMACS_AD_SOURCE}'
echo ' */'
sh '${AD2C_SCRIPT}' '${EMACS_AD_SOURCE}'
")

add_custom_command(
  OUTPUT "${EMACS_AD_H}"
  DEPENDS "${EMACS_AD_SOURCE}" "${AD2C_SCRIPT}"
  COMMAND sh "${GEN_EMACS_AD_SCRIPT}" > "${EMACS_AD_H}"
  COMMENT "Generating Emacs.ad.h from etc/Emacs.ad"
)

add_custom_target(Emacs_ad_h ALL DEPENDS "${EMACS_AD_H}")

set(GENERATED_HEADERS
  "${CMAKE_BINARY_DIR}/src/config.h"
  "${CMAKE_BINARY_DIR}/src/paths.h"
  "${CMAKE_BINARY_DIR}/src/Emacs.ad.h"
)

include_directories(
  "${CMAKE_SOURCE_DIR}/src"
  "${CMAKE_BINARY_DIR}/src"
  "${CMAKE_SOURCE_DIR}/lib-src"
  "${CMAKE_BINARY_DIR}/lwlib"
)

set(CMAKE_RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/lib")
set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/lib")

add_subdirectory(lib-src)
add_subdirectory(lwlib)
add_subdirectory(src)

if(XEMACS_WITH_MODULES)
  add_subdirectory(modules)
endif()

add_subdirectory(tests)

include("${CMAKE_SOURCE_DIR}/cmake/XEmacsCPack.cmake")

configure_file(
  "${CMAKE_SOURCE_DIR}/cmake/Installation.txt.in"
  "${CMAKE_BINARY_DIR}/Installation.txt"
  @ONLY
)
# Also generate without .txt extension - needed during pdump
# Use build-time operation to support out-of-tree builds correctly
add_custom_command(
  OUTPUT "${CMAKE_BINARY_DIR}/Installation"
  DEPENDS "${CMAKE_BINARY_DIR}/Installation.txt"
  COMMAND ${CMAKE_COMMAND} -E copy_if_different
    "${CMAKE_BINARY_DIR}/Installation.txt"
    "${CMAKE_BINARY_DIR}/Installation"
  COMMENT "Generating Installation file"
  VERBATIM
)

add_custom_target(generate_installation ALL
  DEPENDS "${CMAKE_BINARY_DIR}/Installation"
)

if(XEMACS_INSTALL_LISP)
  if(IS_DIRECTORY "${CMAKE_SOURCE_DIR}/lisp")
    install(DIRECTORY lisp/
      DESTINATION ${XEMACS_LISPDIR}
      COMPONENT Lisp
      FILES_MATCHING
        PATTERN "*.el"
        PATTERN "*.elc"
        PATTERN "*.el.gz"
        PATTERN "*.elc.gz"
      PATTERN "CVS" EXCLUDE
      PATTERN ".git" EXCLUDE
      PATTERN ".svn" EXCLUDE
    )
  endif()
endif()

if(XEMACS_INSTALL_ETC)
  if(IS_DIRECTORY "${CMAKE_SOURCE_DIR}/etc")
    install(DIRECTORY etc/
      DESTINATION ${XEMACS_ETCDIR}
      COMPONENT Etc
      PATTERN "CVS" EXCLUDE
      PATTERN ".git" EXCLUDE
      PATTERN ".svn" EXCLUDE
    )
  endif()
endif()

if(XEMACS_INSTALL_INFO)
  if(IS_DIRECTORY "${CMAKE_SOURCE_DIR}/info")
    install(DIRECTORY info/
      DESTINATION ${XEMACS_INFODIR}
      COMPONENT Info
      FILES_MATCHING
        PATTERN "*.info"
        PATTERN "*.info-[0-9]"
        PATTERN "*.info-[0-9][0-9]"
      PATTERN "CVS" EXCLUDE
      PATTERN ".git" EXCLUDE
      PATTERN ".svn" EXCLUDE
    )
  endif()
endif()

if(XEMACS_INSTALL_DOC)
  if(IS_DIRECTORY "${CMAKE_SOURCE_DIR}/man")
    install(DIRECTORY man/
      DESTINATION ${XEMACS_MANDIR}/man1
      COMPONENT Documentation
      FILES_MATCHING
        PATTERN "*.1"
      PATTERN "CVS" EXCLUDE
      PATTERN ".git" EXCLUDE
      PATTERN ".svn" EXCLUDE
    )
  endif()
  
  install(FILES
    "${CMAKE_BINARY_DIR}/Installation.txt"
    DESTINATION ${XEMACS_DOCDIR}
    COMPONENT Documentation
    RENAME "Installation"
  )
  
  if(EXISTS "${CMAKE_SOURCE_DIR}/INSTALL")
    install(FILES
      "${CMAKE_SOURCE_DIR}/INSTALL"
      DESTINATION ${XEMACS_DOCDIR}
      COMPONENT Documentation
    )
  endif()
  
  if(EXISTS "${CMAKE_SOURCE_DIR}/README")
    install(FILES
      "${CMAKE_SOURCE_DIR}/README"
      DESTINATION ${XEMACS_DOCDIR}
      COMPONENT Documentation
    )
  endif()
  
  if(EXISTS "${CMAKE_SOURCE_DIR}/COPYING")
    install(FILES
      "${CMAKE_SOURCE_DIR}/COPYING"
      DESTINATION ${XEMACS_DOCDIR}
      COMPONENT Documentation
    )
  endif()
  
  if(EXISTS "${CMAKE_SOURCE_DIR}/NEWS")
    install(FILES
      "${CMAKE_SOURCE_DIR}/NEWS"
      DESTINATION ${XEMACS_DOCDIR}
      COMPONENT Documentation
    )
  endif()
endif()

install(DIRECTORY
  DESTINATION ${XEMACS_SITELISPDIR}
  COMPONENT Lisp
)

install(DIRECTORY
  DESTINATION ${XEMACS_SITEMODULEDIR}
  COMPONENT Libraries
)

message(STATUS "")
message(STATUS "========== Configuration Summary ==========")
message(STATUS "")
message(STATUS "XEmacs version: ${EMACS_VERSION}")
message(STATUS "Codename: ${XEMACS_CODENAME}")
message(STATUS "")

# --- Installation options ---
message(STATUS "--- Installation options ---")
message(STATUS "  prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "  bindir: ${XEMACS_BINDIR}")
message(STATUS "  datadir: ${XEMACS_DATADIR}")
message(STATUS "  libdir: ${XEMACS_LIBDIR}")
message(STATUS "  lispdir: ${XEMACS_LISPDIR}")
message(STATUS "  etcdir: ${XEMACS_ETCDIR}")
message(STATUS "  archlibdir: ${XEMACS_ARCHLIBDIR}")
message(STATUS "")

# --- Window system ---
message(STATUS "--- Window system ---")

# X11
if(HAVE_X_WINDOWS)
  message(STATUS "  X11: enabled")
  message(STATUS "    Font rendering: ${X11_FONT_RENDERING}")
  if(HAVE_MENUBARS)
    message(STATUS "    Menubars: ${X11_MENUBARS_TYPE}")
  else()
    message(STATUS "    Menubars: disabled")
  endif()
  if(HAVE_XFT_TABS)
    message(STATUS "    Tabs: enabled (Xft)")
  else()
    message(STATUS "    Tabs: disabled")
  endif()
  if(HAVE_XFT_GAUGES)
    message(STATUS "    Gauges: enabled (Xft)")
  else()
    message(STATUS "    Gauges: disabled")
  endif()
  if(HAVE_SCROLLBARS)
    message(STATUS "    Scrollbars: ${XEMACS_WITH_SCROLLBARS_TYPE}")
  else()
    message(STATUS "    Scrollbars: disabled")
  endif()
  if(HAVE_TOOLBARS)
    message(STATUS "    Toolbars: enabled")
  else()
    message(STATUS "    Toolbars: disabled")
  endif()
else()
  message(STATUS "  X11: disabled")
endif()

# GTK
if(XEMACS_WITH_GTK)
  message(STATUS "  GTK: enabled")
else()
  message(STATUS "  GTK: disabled")
endif()

# GNOME
if(XEMACS_WITH_GNOME)
  message(STATUS "  GNOME: enabled")
else()
  message(STATUS "  GNOME: disabled")
endif()

# MS Windows
if(XEMACS_WITH_MSW)
  message(STATUS "  MS Windows: enabled")
else()
  message(STATUS "  MS Windows: disabled")
endif()
message(STATUS "")

# --- TTY ---
message(STATUS "--- TTY ---")
if(HAVE_TTY)
  message(STATUS "  TTY: enabled")
  if(HAVE_NCURSES)
    message(STATUS "  ncurses: enabled")
  endif()
else()
  message(STATUS "  TTY: disabled")
endif()
message(STATUS "")

# --- Image formats ---
message(STATUS "--- Image formats ---")
message(STATUS "  PNG: ${HAVE_PNG}")
message(STATUS "  JPEG: ${HAVE_JPEG}")
message(STATUS "  TIFF: ${HAVE_TIFF}")
message(STATUS "  GIF: ${HAVE_GIF}")
message(STATUS "  XPM: ${HAVE_XPM}")
message(STATUS "")

# --- System ---
message(STATUS "--- System ---")
message(STATUS "  System: ${MACHINE_NAME}-${OPSYS_NAME}")
message(STATUS "")

# --- Dump ---
message(STATUS "--- Dump ---")
if(XEMACS_WITH_DUMP_IN_EXEC AND NOT WIN32)
  message(STATUS "  DUMP_IN_EXEC: enabled (embedding dump data into executable)")
else()
  message(STATUS "  DUMP_IN_EXEC: disabled")
endif()
message(STATUS "")

message(STATUS "===========================================")
message(STATUS "")
